/**
	@description	Deletes the background from memory, freeing the memory used.
	@param			{real} index	The index of the background to be deleted.
	@returns		{undefined}
*/
function background_delete(index) {
	sprite_delete(index);
}
