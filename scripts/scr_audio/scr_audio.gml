/**
	@description	Returns the sound representing the intro part of the specified sound.
	@param			{Asset.GMSound} sound	The sound to get the intro for
	@returns		{Asset.GMSound | real}
*/
function sound_get_intro(sound) {
	switch (sound) {
		default:
			return -1;
	}
}