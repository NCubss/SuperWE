/**
	@description	Determines whether a background exists or not.
	@param			{real} index	The index of the background to check.
	@returns		{bool}
*/
function background_exists(index) {
	return sprite_exists(index);
}
